enum SectionType {
  home,
  publicaciones_categoria,
  publicaciones,
  recetas,
  pois,
  pois_ciudad,
  wiki,
  followed,
}

enum ContentSortType {
  fecha_creacion,
  fecha_actualizacion,
  puntuacion,
  proximidad,
  vistas,
}

enum DeferredExecutorStatus {
  none,
  executing,
  success,
  failed,
}
